{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    },
    "commands": {
      "install": "npm install $NPM_FLAGS"
    }
  }
}
